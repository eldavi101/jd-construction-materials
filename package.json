{
  "name": "jd-construction-materials",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "workspaces": ["apps/*","packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:api": "npm --workspace apps/api run build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "start:api": "npm --workspace apps/api run prisma:migrate:deploy && npm --workspace apps/api run start:prod",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "turbo": "^2.9.12",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  }
}